<?xml version="1.0" encoding="utf-8"?>
<!--
    item_card_small.xml — FULL PREMIUM DESIGN
    Restored height and patterns to match sleek banking mockup.
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="180dp"
    android:layout_marginEnd="16dp"
    app:cardBackgroundColor="@color/purple_primary"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="6dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_card_purple"
        android:padding="20dp">

        <!-- NFC icon top-left -->
        <TextView
            android:id="@+id/ivNfc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/label_bracket"
            android:textColor="#AAFFFFFF"
            android:textSize="16sp"
            android:rotation="-90"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Card Type Logo (VISA/Mastercard) -->
        <ImageView
            android:id="@+id/ivSmallCardLogo"
            android:layout_width="48dp"
            android:layout_height="32dp"
            android:scaleType="fitCenter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_mastercard" />

        <!-- Full masked number -->
        <TextView
            android:id="@+id/tvSmallCardNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:letterSpacing="0.08"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="1253  5432  3521  3090" />

        <!-- Holder name -->
        <TextView
            android:id="@+id/tvSmallCardName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="Ali Hassan" />

        <!-- Expiry -->
        <TextView
            android:id="@+id/tvExpiry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#CCFFFFFF"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvSmallCardName"
            android:layout_marginTop="2dp"
            tools:text="Exp 09/26" />

        <!-- Decorative circle bottom-right -->
        <View
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:background="@drawable/shape_card_circle"
            android:alpha="0.2"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginEnd="-20dp"
            android:layout_marginBottom="-20dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
